#!/bin/bash

# update zsh plugins
echo -e $'\e[33;1mupdate zsh plugins!\e[m'

# update plugins
sheldon lock --update

# reload sheldon
# shellcheck disable=SC1091
source "$XDG_CONFIG_HOME"/zsh/.zshrc
