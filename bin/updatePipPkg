#!/bin/bash

# update pip Packages
echo -e $'\e[33;1mupdate pip Packages!\e[m'

# update packages
pip-review --auto

## install insufficient pip packages
pip install -r "$HOME"/work/pkglist/pippkglist.txt
